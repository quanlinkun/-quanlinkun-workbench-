# 2026-08-27 工作日志

## 雅思板块：接入「每日真题 feed」（方案3 接真实数据源）
- 用户问"题目会每天更新吗"→ 原静态题库不更新；新闻每日更新。
- 选定方案3：联网抓取近期真实雅思考题，每日刷新。
- 新增 `assets/data/ielts-feed.json`：基于 WebSearch 抓到的 2026 真实考题话题（allthingsielts / ieltsupdatesandrecentexams / examword / simplyielts 等）生成当日练习（阅读/听力/口语/写作/每日精读），schema 与 IE_CONTENT 一致。
- 改 `assets/js/ielts.js`：`ieLoadFeed()` 启动优先 fetch feed 覆盖静态题，失败回退内置；仪表盘加「今日真题已联网更新」徽标；词句库加「今日写作真题」卡片。`renderIelts` 改为 async。
- 创建每日自动化「雅思每日真题刷新」（recurring, FREQ=DAILY;BYHOUR=6，project cwd），每天联网重写 feed。
- 校验：node --check 通过；feed schema 与渲染函数依赖一致；本地 http://127.0.0.1:8123 返回 200。
- git 提交 94b3fe4。

## 数学板块：课程上传新增 Word/PDF 支持
- 用户要求：数学「添加课程」支持 Word 文档和 PDF。
- 引入浏览器端解析库并落地于 `assets/js/vendor/`：mammoth.browser.min.js(634KB, 解析 docx)、pdf.min.js+pdf.worker.min.js(解析 pdf)。已用真实 docx/pdf 验证：mammoth 提取文字成功；pdf.js 加载并暴露 v3 API。
- index.html 在 app.js 前加载 vendor；sw.js 缓存升级 v2 并预缓存 vendor 文件（离线可用）。
- 改 `assets/js/app.js`：`mcUpload` 按扩展名分发（txt/md 读文本、docx mammoth 提取、pdf pdf.js 逐页提取），解析显示状态；课程卡片加 Word/PDF 徽标 + 「下载原文件」（<4MB 存 base64）；`mcSave` 增加 localStorage 配额保护（超限自动去 blob 重试）。
- 校验：node --check 通过；本地 8123 各文件 200。已 git 提交。

## 部署提示（用户需知）
- 纯静态 + GitHub Pages。feed 在本地/部署后由应用运行时读取；自动化只更新本地文件，需 commit+push 才能让线上站点拿到新题。
- 若用户改用远程 feed URL（如 gist/CDN）可实现线上真正每日更新，但需额外配置。
